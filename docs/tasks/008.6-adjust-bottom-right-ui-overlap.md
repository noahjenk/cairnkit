# Task 8.6: Adjust bottom-right UI overlap

## Goal

Fix the visible overlap between bottom-right map UI elements before continuing to the mock building data source.

## Why this matters

The app shell should keep map controls and app status controls readable and clickable. The bottom-right corner currently has competing UI elements, which makes the map-first layout feel less polished.

## Relevant docs

- `docs/PRODUCT_BRIEF.md`
- `docs/ARCHITECTURE.md`
- `docs/ROADMAP.md`
- `docs/COPILOT_WORKFLOW.md`

## Scope

This task should include:

- adjusting the bottom-right status indicator placement
- preserving MapLibre navigation controls
- preserving the existing mobile layout behavior
- keeping the change limited to layout and styling

This task should not include:

- mock building data
- Rural Area Finder UI
- layer rendering
- saved places
- Overpass
- new app features

## Files likely involved

- `src/app/app.css`

## Implementation steps

1. Identify the overlapping bottom-right elements.
2. Move the app status indicator away from the MapLibre navigation control stack.
3. Keep mobile placement readable and non-overlapping.
4. Confirm TypeScript builds.

## Acceptance criteria

- [x] Bottom-right app UI no longer overlaps MapLibre controls.
- [x] Status indicator remains visible.
- [x] Mobile status indicator placement remains left-aligned.
- [x] No mock data or new feature behavior has been added.
- [x] App still builds.

Completion note: accepted as a completed UI overlap fix. The status indicator sits left of the MapLibre navigation controls near the bottom edge. Task 009 remains the next implementation task.

## Testing instructions

Run:

```bash
npm run build
```

Then run the app locally and visually confirm the bottom-right controls no longer overlap.

## Notes for Copilot

Keep this task deliberately small. Task 009 remains the mock building data source after this overlap fix is complete.
