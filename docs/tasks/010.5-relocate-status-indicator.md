# Task 10.5: Relocate status indicator

## Goal

Move the app status indicator away from the bottom-right map control area.

## Why this matters

MapLibre controls naturally occupy the bottom-right corner. Keeping the app status indicator there makes overlap likely as map controls or future app controls grow.

## Relevant docs

- `docs/PRODUCT_BRIEF.md`
- `docs/ARCHITECTURE.md`
- `docs/ROADMAP.md`
- `docs/COPILOT_WORKFLOW.md`

## Scope

This task should include:

- moving the desktop status indicator to a more suitable location
- avoiding the bottom-right MapLibre control stack
- preserving the existing mobile behavior if it remains readable
- keeping the change limited to layout and styling

This task should not include:

- Rural Area Finder radius persistence
- layer rendering
- saved places
- Overpass
- new app features

## Files likely involved

- `src/app/app.css`

## Implementation steps

1. Move the desktop status indicator out of the bottom-right corner.
2. Keep it visible and readable over the map.
3. Confirm the mobile placement still avoids overlap.
4. Confirm TypeScript builds.

## Acceptance criteria

- [x] Desktop status indicator no longer uses the bottom-right map-control area.
- [x] Status indicator remains visible.
- [x] Mobile status indicator remains readable.
- [x] No new feature behavior has been added.
- [x] App still builds.

Completion note: accepted as a completed UI layout task. The status indicator now sits at the top-right of the map workspace on desktop. Task 011 remains the next implementation task.

## Testing instructions

Run:

```bash
npm run build
```

Then run the app locally and confirm the status indicator no longer competes with bottom-right map controls.
