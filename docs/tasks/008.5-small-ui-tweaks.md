# Task 8.5: Small UI tweaks

## Goal

Make two small layout adjustments before continuing to the mock building data source.

## Why this matters

The map-first shell should feel tidy before the next data and tool foundations are added. These tweaks keep the visible UI simple and align the search placeholder with the workspace controls.

## Relevant docs

- `docs/PRODUCT_BRIEF.md`
- `docs/ARCHITECTURE.md`
- `docs/ROADMAP.md`
- `docs/COPILOT_WORKFLOW.md`

## Scope

This task should include:

- moving the search box placeholder so it sits directly above the workspace panel on the left
- making the top bar thinner
- simplifying the top bar so it says only `CairnKit`
- preserving the map-first layout
- preserving existing placeholder panels and controls

This task should not include:

- mock building data
- Rural Area Finder UI
- layer rendering
- saved places
- Overpass
- new app features

## Files likely involved

- `src/app/layout/TopBar.tsx`
- `src/app/layout/FloatingSearchBox.tsx`
- `src/app/layout/WorkspacePanel.tsx`
- `src/app/app.css`

## Implementation steps

1. Update the top bar component so it only renders the CairnKit name.
2. Reduce the top bar height and padding.
3. Reposition the floating search placeholder above the left workspace panel.
4. Adjust workspace panel spacing if needed so the search box and panel read as one left-side control stack.
5. Check the mobile layout still avoids overlap.
6. Confirm TypeScript builds.

## Acceptance criteria

- [x] Top bar is thinner than the current version.
- [x] Top bar visible text is only `CairnKit`.
- [x] Search placeholder sits directly above the workspace panel on the left.
- [x] Workspace panel still displays Active Tools, Layers, and Saved Places.
- [x] No mock data or new feature behavior has been added.
- [x] App still builds.

Completion note: accepted as a completed UI-tweak task. Task 009 remains the next implementation task.

## Testing instructions

Run:

```bash
npm run build
```

Then run the app locally and visually confirm the desktop and mobile layouts.

## Notes for Copilot

Keep this task deliberately small. Task 009 remains the mock building data source after these UI tweaks are complete.
